IF(VEDATensors_DIST_TYPE STREQUAL LOCAL)
	INCLUDE(${CMAKE_CURRENT_LIST_DIR}/local.cmake)
ELSEIF(VEDATensors_DIST_TYPE STREQUAL PYTHON)
	INCLUDE(${CMAKE_CURRENT_LIST_DIR}/python.cmake)
ELSE()
	MESSAGE(FATAL_ERROR "Unsupported VEDATensors_DIST_TYPE=${VEDATensors_DIST_TYPE}")
ENDIF()
